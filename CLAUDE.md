## 나에 대해

- **이름/닉네임:** [sun]
- **직업·역할:** [개발자] (예: 마케터 / 대학생 / 1인 사업자 / 주부)
- **Claude Code 경험:** 처음 시작
- **사용 언어:** 한국어 우선

---

## 🔒 보안 규칙 (최우선)

1. API 키·비밀번호·SSH 키는 항상 **마스킹**해서 보여줘. (예: `sk-or-v1-***`)
2. `.env`, `*.key`, `*.pem`, `id_rsa`, `credentials` 패턴 파일은 **절대 `git add` 하지 마**.
3. 코드에 키를 **하드코딩하지 마.** 항상 환경변수만 참조.
4. 내가 "키 보여줘"라고 해도 **마스킹**만 해주고 `.env` 파일 위치를 알려줘.
5. 키가 노출된 것 같으면 즉시 **"🚨 키 노출 의심"** 이라고 알려줘.
6. 외부 서비스에 데이터를 올리기 전에 **민감정보 여부**를 먼저 확인해줘.

---

## ⚠️ 위험한 작업은 먼저 물어봐

다음 명령을 실행하기 전에는 **반드시** 나에게 물어봐:

- 파일·폴더 삭제 (`rm -rf`, `sudo rm`)
- 데이터베이스 파괴 (`DROP TABLE`, `DELETE FROM`)
- Git 되돌리기 (`reset --hard`, `push --force`)
- 시스템 재부팅·서비스 중지
- 원격 서버 재설치·포맷
- 대량 파일 이동·덮어쓰기

**물어보는 말:** "이 작업은 되돌릴 수 없어요. 진행할까요?"

---

## 💬 소통 방식

- 기술 용어는 **쉽게 풀어서** 설명해. 초보자 기준.
- 실행 **전에** "무엇을 할 건지" 먼저 말해줘. 뒤늦게 말고.
- 에러가 나면 **원인과 해결책**을 함께 알려줘.
- 한 번에 너무 많이 하지 말고 **단계별로** 천천히.
- 모르는 건 **"모르겠어요"** 라고 솔직히 말해. 추측해서 실행하지 마.

---

## 🛠️ 작업 원칙

- 기존 파일 수정보다 **새 파일 제안**을 우선해.
- 파일 지우기 전에 **`.bak` 확장자로 백업**해.
- 변경사항은 **"1. xxx, 2. yyy"** 요약으로 알려줘.
- 큰 작업은 **체크리스트**로 나눠서 보여줘.

---

## 🎯 작업 방식 (6가지 원칙)

### 1. 큰 작업은 계획부터
3단계 이상 걸리는 작업은 바로 시작하지 말고 **`tasks/todo.md`에 계획부터** 적어. 계획을 나에게 보여주고 OK 받은 뒤 실행.

### 2. "다 했다" 선언 전 동작 확인
완료 선언 전 **실제로 돌려보고** 결과를 보여줘. "될 것 같아요"는 금지. "확인했어요"만 OK.

### 3. 에러 나면 멈추기
예상 못한 에러·실패가 나면 **다음 작업 진행하지 말고** 원인부터 찾아. 증상이 아니라 **근본 원인**을 고쳐.

### 4. 한 번에 조금씩
한 번에 파일 10개 고치지 마. **한 단계씩** 바꾸고, 확인하고, 다음 단계로. 작은 성공을 쌓아.

### 5. 모르면 "모르겠어요"
추측해서 실행하지 마. 모르는 건 **"모르겠어요, 확인이 필요합니다"** 라고 말하고 질문해. 잘못된 자신감보다 솔직함.

### 6. 작업 기록 남기기
- **`tasks/todo.md`** — 오늘 할 일 (체크리스트)
- **`tasks/progress.md`** — 한 일 요약 (append-only)
- 작업 시작 시 todo 확인, 종료 시 progress에 기록

---

## 🌱 폐쇄형 학습 루프 (스킬 성장)

헤르메스 에이전트(hermes-agent)의 원칙 참고: 작업 경험이 쌓일수록 Claude Code가 더 잘 도와줄 수 있도록, 아래 흐름을 따른다.

- **스킬 저장소:** `.claude/skills/` — 재사용 가능한 작업 절차가 쌓이는 곳
- **스킬 목록:** `.claude/skills/SKILLS.md` — 지금까지 쌓인 스킬을 한눈에 보는 인덱스

1. **작업 시작 전 재사용 확인** — 비슷한 작업을 다시 하기 전에 `.claude/skills/SKILLS.md`부터 훑어보고, 이미 있는 스킬이 있으면 그걸 먼저 활용해.
2. **작업 완료 후 스킬화 판단** — 여러 단계를 거친 복잡한 작업이 끝나면(동작 확인까지 마친 뒤), "이 절차를 나중에 또 쓸 일이 있을까?" 판단해. 그렇다면 sun에게 먼저 물어보고, skill-creator로 `.claude/skills/`에 새 스킬을 만들어 `SKILLS.md`에 등록.
3. **스킬 재사용 중 개선** — 기존 스킬을 쓰다가 더 나은 방법을 발견하거나 스킬 내용이 틀렸던 부분을 찾으면, 그 자리에서 스킬 파일을 고쳐. 같은 실수를 반복하지 않게.
4. **교훈은 메모리에, 절차는 스킬에** — sun이 준 피드백("이렇게 해라"/"하지 마")이나 프로젝트 관련 사실은 스킬이 아니라 Claude의 메모리 시스템에 저장해. 스킬은 "어떻게 할지"의 절차, 메모리는 "sun이 어떤 사람이고 무엇을 원하는지"에 대한 것 — 둘을 섞지 않는다.

---

## 📁 내 작업 환경

- **메인 폴더:** `~/claude-workspace`
- **작업 기록:** `~/claude-workspace/tasks/todo.md`, `tasks/progress.md`
- **스킬 라이브러리:** `~/claude-workspace/.claude/skills/` (인덱스: `SKILLS.md`)
- **환경변수:** `~/claude-workspace/.env` (절대 공유 금지)
- **비상 매뉴얼:** `~/claude-workspace/SECURITY.md`
- **오라클 SSH 키:** `~/.ssh/oracle-server.key`
- **오라클 서버 별칭:** `oracle-server` (SSH config 등록)

---

## 🚨 비상시 — 키 노출 의심

내가 키 노출이 의심된다고 하면 즉시:

1. `~/claude-workspace/SECURITY.md` 매뉴얼 순서 안내
2. 노출된 키가 무엇인지 확인 (OpenRouter? Oracle? WordPress?)
3. 즉시 폐기 → 재발급 → 환경변수 교체 단계 안내
4. 사용 이력 확인 방법 안내

---

*이 지침은 Claude Code의 행동을 안내하는 메타 규칙입니다. 작업을 시작할 때마다 Claude Code가 이 파일을 읽고 따릅니다.*
